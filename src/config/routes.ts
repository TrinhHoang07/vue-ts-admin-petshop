const routesConfig = {
    home: '/',
    products: '/products',
    customers: '/customers',
    chat: '/chat',
    login: '/login',
    orders: '/orders',
    notFound: '/:pathMatch(.*)*',
};

export default routesConfig;
